print "hello"

//def startTime = String.format("%s.%s", DateUtils.addHours(new Date(), -(1)).format("yyyy-MM-dd HH:mm:ss"), "000")
//def endTime = String.format("%s.%d", new Date().format("yyyy-MM-dd HH:mm:ss"), 999)
//println startTime
//println endTime
//def command = /awk '$0 >= "$startTime" && $0 <= "$endTime"' $logFilePath/

int i = 0
if(i)
println "true"
else
println "false"
